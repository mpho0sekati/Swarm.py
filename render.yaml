services:
  - type: web
    name: dark-swarm-factory
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run Main.py --server.port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
